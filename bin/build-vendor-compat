#!/usr/bin/env bash

cd "${BASH_SOURCE%/*}/.."

rm -rf build-vendor

cp composer.json composer-compat.json

COMPOSER=composer-compat.json COMPOSER_VENDOR_DIR=build-vendor \
  composer require symfony/intl hoa/console --no-progress --no-update

COMPOSER=composer-compat.json COMPOSER_VENDOR_DIR=build-vendor \
  composer update --prefer-stable --no-dev --no-progress --classmap-authoritative

